{
  "name": "glicolog-vue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "build:apk": "CAPACITOR=1 vite build && npx cap sync android && cd android && ANDROID_HOME=/home/andrealazzari/android-sdk JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 ./gradlew assembleDebug",
    "cap:sync": "CAPACITOR=1 vite build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/cli": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "capacitor-health-connect": "^0.7.0",
    "pinia": "^2.1.7",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.2",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.19.0"
  }
}
